export const metadata = {
    title: "AI Models - Frenix",
    description: "Explore all available AI models from leading providers on Frenix. Choose from Claude, GPT, GLM, DeepSeek, Grok, and more.",
};

export default function ModelsLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
